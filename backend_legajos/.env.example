# Ejemplo de variables de entorno para backend_legajos
# Copia este archivo a .env y ajusta valores reales.

# Base de datos Postgres (Railway o local Docker)
DATABASE_URL=postgresql://usuario:password@localhost:5432/legajosdb?schema=public

# Secreto para firma JWT
JWT_SECRET=changeme-local-secret

# Origin permitido para CORS (frontend)
CORS_ORIGIN=http://localhost:5000

# Credenciales semilla sysadmin (solo primera creación o recuperación)
ADMIN_EMAIL=sysadmin@test.com
ADMIN_NAME=SysAdmin
ADMIN_PASSWORD=sys123

# Control de auto-seed al arrancar. true = fuerza seed siempre.
# Si se deja en false, se hará seed sólo si no existe ningún sysadmin.
AUTO_SEED_ADMIN=false

# NOTAS:
# - En producción cambia todos estos valores.
# - No mantengas AUTO_SEED_ADMIN=true indefinidamente.
# - Si la contraseña contiene caracteres especiales en Railway, evita comillas dobles innecesarias.
